# Бэклог известных проблем

**Последнее обновление:** 2026-01-30

---

## Исправлено (30 января 2026) - Code Review

### Архитектура
- [x] Удалён дублирующий плагин `nine/plugins/dnd_audio/` (оставлен `nine/plugins/dnd/audio/`)
- [x] Создана документация систем Entity (`docs/ENTITY_SYSTEMS.md`)
- [x] Тестовые файлы перемещены из корня в `tests/` директорию
- [x] Создан автоматический инструмент проверки архитектуры (`tools/architecture_check.py`)
- [x] Добавлена проверка дубликатов unique_id в PluginManager

### UI / Интерфейс
- [x] Исправлена кнопка удаления (X) в Character Select - увеличен масштаб, правильный frameSize
- [x] Исправлена кнопка "Играть" - уменьшена ширина, смещена позиция для предотвращения наложения
- [x] Исправлено позиционирование "Последняя игра" - переключено на left-align, правильная позиция
- [x] Унифицированы параметры шрифтов - fallback использует те же setPixelsPerUnit
- [x] Создан инструмент проверки UI (`tools/ui_check.py`)
- [x] Проведён полный анализ UI системы (найдено 10 багов, 4 критических исправлено)

---

## Требует тестирования

### Музыка (audio_manager.py) — январь 2026
- [ ] Проверить что треки плейлиста переключаются корректно (не обрываются)
- [ ] Проверить crossfade между треками
- [ ] Проверить что BGM не останавливается через несколько секунд
- [ ] Тест: `play_bgm("adventure")` — должен играть несколько треков подряд

### Камера с коллизией (camera_controller.py) — январь 2026
- [ ] Проверить что камера не проходит сквозь стены
- [ ] Проверить что камера отодвигается от стен на 0.3 юнита
- [ ] Проверить работу в узких коридорах
- [ ] Проверить что `min_distance` соблюдается при коллизии
- [ ] Если коллизия не работает — проверить `CollideMask` на геометрии карты

### UI: Выбор персонажа (cl_character_select.py) — январь 2026
- [x] ~~Проверить что кнопка X не налазит на кнопку "Играть"~~ — исправлено (scale 0.05, позиция сдвинута)
- [ ] Проверить что BG1 стиль кнопок отображается корректно
- [x] ~~Проверить что текст не обрезается в карточках персонажей~~ — исправлено (ширина кнопки уменьшена)
- [x] ~~Проверить hover-эффект на кнопке удаления (X становится ярче)~~ — работает корректно

### Шрифты (theme.py, manager.py) — январь 2026
- [ ] Проверить четкость пиксельного шрифта PlainPixel (требует визуального теста)
- [ ] Проверить что кириллица отображается корректно (требует визуального теста)
- [x] ~~Если символы размыты — проверить что FONT_USE_NEAREST = True~~ — проверено, работает
- [x] ~~Если символы отсутствуют — использовать DejaVuSans.ttf (fallback)~~ — fallback настроен корректно

---

## Критические

### NPC System - Множественные критические проблемы (30 января 2026)
**Статус:** Открыто
**Приоритет:** КРИТИЧЕСКИЙ (блокирует геймплей)
**Файлы:** `nine/plugins/npc/cl_npc_renderer.py`, `nine/plugins/combat/sv_turn_manager.py`, `nine/plugins/npc/sv_npc_ai.py`

**Описание:** После тестирования NPC системы выявлено 7 критических проблем:

1. **NPC RGB проблема** - NPC отображается синим вместо зелёного
   - Добавлен color_map в cl_npc_renderer.py:89-100
   - setColor() вызывается но цвет не применяется
   - Возможная причина: модель base.bam имеет текстуру которая перекрывает цвет
   - Файл: `nine/plugins/npc/cl_npc_renderer.py:97`

2. **NPC мигает при движении** - пропадает на мгновение
   - Проблема синхронизации/рендеринга
   - Возможно слишком частые/редкие обновления world_state
   - Файл: `nine/plugins/npc/cl_npc_client.py`

3. **T-поза у NPC** - передвигается в T позе без анимаций
   - Actor создаётся но анимации не загружаются/не воспроизводятся
   - Возможная причина: base.bam не содержит анимаций, нужны .egg файлы
   - Файл: `nine/plugins/npc/cl_npc_renderer.py:84-92`

4. **Бой начинается автоматически** - NPC атакует хотя behavior=NEUTRAL
   - Изменены templates на NEUTRAL (goblin, skeleton)
   - НО проблема осталась - AI всё равно агрится
   - Возможная причина: AI логика не проверяет behavior перед началом боя
   - Файл: `nine/plugins/npc/sv_npc_ai.py`

5. **Можно бегать не в свой ход** - turn management не работает
   - Нет блокировки движения во время чужого хода
   - Игрок может двигаться когда ход NPC
   - Файл: `nine/plugins/combat/sv_turn_manager.py`, `client.py` (input blocking)

6. **Бесконечный ход у NPC** - ход не переключается
   - NPC получает ход и он не заканчивается
   - Turn manager не переключает на следующего участника
   - Файл: `nine/plugins/combat/sv_turn_manager.py`

7. **NPC не спавнится** - использовалась неправильная команда
   - ИСПРАВЛЕНО: команда `/spawn` для предметов, `/spawnnpc` для NPC
   - Статус: ✅ FIXED в тестах

**Решение (план исследования):**
- Проверить почему setColor() не работает на base.bam модели
- Проверить частоту world_state обновлений
- Проверить загрузку анимаций в Actor
- Проверить AI логику на правильность проверки behavior
- Добавить блокировку input во время чужого хода
- Исправить turn switching логику

**Приоритет исправления:**
1. MUST FIX: RGB, Turn management, Автобой
2. SHOULD FIX: T-поза, Мигание
3. FIXED: Команда спавна

---

### UI System - Множественные проблемы наложения (январь 2026)
**Статус:** Открыто (отложено до полного рефакторинга UI)
**Приоритет:** Средний (не блокирует геймплей)
**Файлы:** `nine/ui/blocks.py`, `nine/ui/login_menu.py`, `nine/ui/settings_menu.py`, `nine/plugins/dnd/cl_character_select.py`

**Описание:** После тестирования обнаружено множество проблем с UI layout системой:
1. Имя персонажа в Settings вылазит за границы окна
2. Кнопки "Войти" и "Назад" в Login Menu налазят на поле ввода пароля
3. Кнопка удаления (X) в Character Select отображается как полоска вместо крестика
4. Общая проблема: элементы интерфейса налазят друг на друга по всему UI

**Причина:** Блочная система layout (`blocks.py`) имеет фундаментальные проблемы с расчётом позиций и размеров:
- `Row.build()` неправильно распределяет элементы по горизонтали
- `EntryElement.get_size()` не учитывает переменную ширину кириллических символов
- `Block.get_content_height()` неправильно суммирует высоты элементов
- Z-ordering не контролируется, элементы перекрывают друг друга

**Решение:** Полный рефакторинг UI layout системы (отложено):
- Заменить самописную блочную систему на Panda3D DirectGUI layout (DirectBoxSizer)
- Или реализовать CSS-подобную flexbox систему
- Добавить автоматическое определение overflow и scroll
- Добавить debug mode с подсветкой границ элементов

**Временное решение:** Использовать прямое позиционирование через BG1Button.create() вместо Block.row()

---

### Архитектура (январь 2026) - Исправлено
- [x] ~~Проверить что плагин `nine.dnd.audio` корректно загружается после удаления дублирующего `dnd_audio`~~ — проверено
- [x] ~~Добавить проверку на дубликаты `unique_id` при загрузке плагинов (в `PluginManager`)~~ — добавлено в plugins.py:241

---

## Средний приоритет

### Камера и управление
- [x] ~~Пофиксить камеру игрока (проблемы с поворотом/следованием)~~ — добавлена коллизия
- [ ] Проверить first-person режим камеры

### UI / Интерфейс
- [ ] Кнопки в главном меню — неверное отображение (требует визуального теста)
- [x] ~~Кнопки в интерфейсе выбора/загрузки персонажа — красный крестик отображается неверно~~ — исправлено в cl_character_select.py
- [ ] Проверить все кнопки на корректность текстур/геометрии (требует визуального теста)
- [ ] Combat UI: 15 hardcoded позиций требуют рефакторинга на UIConfig/ScaledValue (cl_action_bar.py, cl_initiative_display.py)

### NPC система
- [ ] Модели NPC (сейчас отображаются как синие сферы-заглушки)
- [ ] Анимации NPC (idle, walk, attack, death)
- [ ] Диалоговая система для friendly NPC (merchant, guard)
- [ ] NPC patrol визуализация (debug mode)
- [ ] NPC leash radius — возврат домой если ушёл слишком далеко

### Поведение NPC (AI)
- [ ] Гоблин атакует игрока когда в радиусе атаки
- [ ] Страж реагирует на враждебных NPC рядом с игроком
- [ ] NPC убегает при низком HP (flee behavior)
- [ ] NPC группы — координированные атаки
- [ ] Line of sight — NPC не видит сквозь стены

### Боевая система
- [ ] Action Bar не появляется при старте боя (проверить клиентскую часть)
- [ ] Target Selector — подсветка выбранного NPC
- [ ] Урон и броски атаки (D20 + модификаторы)
- [ ] Смерть NPC — удаление из мира, выпадение лута
- [ ] HP бар над NPC в бою
- [ ] Звуки боя (удар, промах, смерть)
- [ ] Визуальные эффекты (кровь, искры при ударе)

### UI боевой системы
- [ ] Initiative tracker (список очерёдности ходов)
- [ ] Combat log (лог действий в бою)
- [ ] Кнопки действий с иконками
- [ ] Подсказки при наведении на действия
- [ ] Индикатор "Ваш ход" / "Ход противника"

---

## Низкий приоритет

### Качество жизни
- [ ] Миникарта с позициями NPC
- [ ] Автосохранение позиции персонажа
- [ ] Настройки громкости звуков боя

---

## Оптимизация NPC (для масштабирования 100-500+ NPC)

> **Цель:** Достичь уровня RimWorld/Kenshi — сотни NPC с индивидуальным поведением.
> **Текущая оценка:** ~20-50 NPC без оптимизации.

### AI Level of Detail (AI LOD)
- [ ] **Distant NPCs think less** — NPC далеко от игрока думают реже (раз в 2-5 сек вместо 0.5)
- [ ] **Offscreen NPCs** — NPC вне экрана используют упрощённую симуляцию
- [ ] **Sleep system** — NPC в неактивных чанках полностью "спят"

### Pathfinding оптимизация
- [ ] **Hierarchical A*** — двухуровневый pathfinding (грубый + детальный)
- [ ] **Path caching** — кэширование часто используемых путей
- [ ] **Flow fields** — для групп NPC идущих в одну точку
- [ ] **Async pathfinding** — расчёт путей в отдельном потоке
- [ ] **Path request throttling** — не более N запросов путей за тик

### Spatial Partitioning
- [ ] **Grid-based chunks** — мир разбит на чанки, обновляются только активные
- [ ] **Spatial hash** — быстрый поиск соседей для AI
- [ ] **Interest management** — клиент получает только nearby NPC

### Batching и Pooling
- [ ] **Entity pooling** — переиспользование Entity объектов
- [ ] **Component pooling** — переиспользование компонентов
- [ ] **Batched updates** — группировка обновлений (не каждый кадр)
- [ ] **Dirty flags** — обновлять только изменённые данные

### Network оптимизация
- [ ] **Delta compression** — отправлять только изменения позиций
- [ ] **Priority queue** — важные NPC (в бою) обновляются чаще
- [ ] **Area of Interest** — клиент получает только NPC в радиусе
- [ ] **Interpolation hints** — клиент предсказывает движение

### Rendering оптимизация
- [ ] **LOD models** — упрощённые модели для дальних NPC
- [ ] **Animation LOD** — дальние NPC используют упрощённые анимации
- [ ] **Instancing** — одинаковые модели рендерятся батчами
- [ ] **Culling** — не рендерить NPC за камерой/стенами
- [ ] **Billboard sprites** — очень дальние NPC как 2D спрайты

### Архитектурные улучшения
- [ ] **Job system** — параллельные обновления систем
- [ ] **Data-oriented ECS** — оптимизация под кэш CPU
- [ ] **C extension** — критичные системы (pathfinding, AI) на C/Cython
- [ ] **Профилирование** — найти реальные bottlenecks

### Целевые метрики
| Метрика | Цель |
|---------|------|
| NPC count | 300-500 одновременно |
| AI update | < 5ms на 100 NPC |
| Pathfinding | < 2ms на запрос |
| Network | < 50KB/s на клиента |
| Frame time | < 16ms (60 FPS) |

---

## Исправлено (январь 2026)

### Unicode/Encoding
- [x] Unicode ошибки в world.py (эмодзи удалены)
- [x] Unicode ошибки в character_controller.py (эмодзи удалены)
- [x] Эмодзи в character_controller.py:250

### Combat плагин
- [x] cl_spectator_mode.py: неправильный импорт globalClock
- [x] cl_spectator_mode.py: self.base -> self.app
- [x] cl_target_selector.py: self.base -> self.app
- [x] cl_action_bar.py: self.base -> self.app
- [x] cl_combat_ui.py: self.base -> self.app

### Audio плагин
- [x] cl_audio_integration.py: self.base -> self.app
- [x] cl_footsteps.py: self.base -> self.app

### Архитектура плагинов
- [x] Относительные импорты в плагинах combat/npc
- [x] Интеграция плагинов combat и npc в систему

### Инвентарь
- [x] Двойной инвентарь при подключении

### NPC система
- [x] NPC не отображались на клиенте - добавлен `world_state_received` event в `client.py:667`

### Архитектура ECS (январь 2026)
- [x] Унификация Player и NPC под единую ECS систему
- [x] Создание `nine/core/components.py` с едиными компонентами (Transform, Pawn, Physics, etc.)
- [x] Создание `nine/core/systems.py` (PhysicsSystem, AISystem, AnimationSystem, NetworkSyncSystem)
- [x] Добавление `query()` метода в `nine/core/ecs.py`
- [x] Интеграция Player в ECS через `GameWorld`
- [x] Режим unified mode в `sv_npc_manager.py` для shared ECS world
- [x] Единый формат `world_state` с `pawns[]` вместо отдельных `players[]` и `npcs[]`

---

## Как добавить проблему

```markdown
### Название проблемы
**Статус:** Открыто | В работе | Исправлено
**Приоритет:** Критический | Средний | Низкий
**Файл:** `путь/к/файлу.py:строка`

**Описание:** Краткое описание проблемы.

**Ошибка:**
\```
Текст ошибки
\```

**Решение:** Предложенное решение (если известно).
```
